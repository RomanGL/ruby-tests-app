<h1>Добавить вопрос к тесту "<%= @quiz.title %>"</h1>

<%= render 'questions/edit_form' %>

<%= link_to 'Вернуться к списку вопросов',
            {
                :controller => 'questions',
                :action => 'index',
                :quiz_id => @quiz.id
            },
            { :class => 'btn btn-default' } %>